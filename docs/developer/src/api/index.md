# Doxygen Output

Doxygen was not generated for this build.

Install Doxygen, rerun CMake so it can be detected, then rebuild:

```sh
cmake -S . -B build-host
cmake --build build-host --target developer_docs
```

When Doxygen is available, this placeholder is replaced by the generated API
reference.
